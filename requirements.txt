# ═══════════════════════════════════════════════════════════════════
#  SmartAttend — Python Dependencies
#  Install: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════════

# Web framework
flask>=3.0.0

# Computer Vision & Face Recognition
opencv-python-headless>=4.9.0    # headless = no GUI — better for Pi
face-recognition>=1.3.0          # wraps dlib's ResNet face model
# insightface>=0.7.3             # optional ArcFace backend for higher accuracy
numpy>=1.26.0

# Raspberry Pi camera (install on Pi only)
# picamera2>=0.3.19               # uncomment on Raspberry Pi OS

# Raspberry Pi GPIO (install on Pi only)
# RPi.GPIO>=0.7.1                 # uncomment for LED/buzzer support

# Database — sqlite3 is stdlib, no extra package needed

# System utilities
Pillow>=10.2.0                   # image loading / manipulation
gunicorn>=21.2.0                 # production WSGI server

# Dev / testing
pytest>=8.0.0
